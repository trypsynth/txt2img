[[repos]]
repo = "builtin"
hooks = [
	{ id = "trailing-whitespace" },
	{ id = "end-of-file-fixer" }
]

[[repos]]
repo = "local"
hooks = [
	{
		id = "zift",
		name = "Zift format",
		entry = "zift . --exclude zig-pkg",
		language = "system",
		types = ["zig"],
		pass_filenames = false
	}
]
